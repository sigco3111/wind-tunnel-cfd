# Editor / OS noise
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Node.js (smoke + QA harnesses don't currently use node_modules, but if a
# future test runner introduces deps locally, keep them out of the repo)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# OpenCode / Claude / Codex agent state caches
.omo/
.aider*
.cursor/
.cody/

# Python (in case anyone runs the optional python -m http.server dev harness
# with a local venv)
__pycache__/
*.pyc
.venv/
venv/

# Misc build / scratch
.cache/
*.log